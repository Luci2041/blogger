server:
  port: 99

# ?????
spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/blogger?serverTimezone=UTC
    username: root
    password: 123456
  servlet:
    multipart:
      max-file-size: 1000MB
      max-request-size: 1000MB
      enabled: true
# mybatisplus??
mybatis-plus:
  global-config:
    db-config:
      table-prefix:
  configuration:
    log-impl: org.apache.ibatis.logging.stdout.StdOutImpl   # mybatisplus????
    map-underscore-to-camel-case: true  #不取消大小写转化加下划线